server:
  port: 8080
spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/statemachinedb
    username: state_machine
    password: postgres123
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

